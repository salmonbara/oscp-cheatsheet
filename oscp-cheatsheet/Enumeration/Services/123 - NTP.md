---
title: 123 - NTP
type: service
tab: Services
ports: [123]
service: NTP
tools: [ntpdate, nmap]
inputs: []
outputs: [TimeSkew, NTPInfo]
tags: [NTP, UDP, Enumeration]
---

# 123 - NTP

`123/udp open ntp`

## Found / Do / Then

- Found: `123/udp open ntp`.
- Do: sync time before Kerberos-heavy AD checks and run safe NTP enum scripts.
- Then: retry Kerberos/LDAP tooling if clock skew was causing failures.

## Commands

### NTP Time Sync

#NTP #UDP #Enumeration
Synchronize local clock with target.

```sh
sudo ntpdate <TARGET_IP>
```

### NTP Nmap Scripts

#NTP #UDP #Enumeration
Enumerate NTP with Nmap scripts.

```sh
nmap -sU -sV --script "ntp* and (discovery or vuln) and not (dos or brute)" -p 123 <TARGET_IP>
```
